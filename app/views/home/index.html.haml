- unless @external_page
  :css
    #tagline:before { background-image: url("#{ContentConfig.url_for(:home_hero)}") }


- content_for :page_alert do
  = render "shared/menu/alert"

%div
  - if @external_page
    = render "home/external_page", page: @external_page
  - else
    - if feature?(:home_products, spree_current_user)
      = render "home/origin_hero"
    - else
      = render "home/tagline"

    #panes
      - if feature?(:home_products, spree_current_user)
        = render "home/products"
        = render "home/origin_steps"
      - else
        -# OFN's own story of local shopping; the marketplace tells its own above.
        = render "home/brandstory"
        = render "home/system"
      = render "home/cta"
      = render "home/stats"

  = render "shared/footer"
